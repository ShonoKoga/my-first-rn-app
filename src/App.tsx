import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  // Alert,
  // AppState,
  AsyncStorage,
} from './components';

const Component = () => {
  return <AsyncStorage />;
};

export default Component;
