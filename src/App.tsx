import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  // Alert,
  // AppState,
  // AsyncStorage,
  // SafeAreaView,
  Progress,
} from './components';

const Component = () => {
  return <Progress />;
};

export default Component;
