import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  ActivityIndicator,
} from './components';

const Component = () => {
  return <ActivityIndicator />;
};

export default Component;
