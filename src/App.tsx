import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  StatusBar,
} from './components';

const Component = () => {
  return <StatusBar />;
};

export default Component;
