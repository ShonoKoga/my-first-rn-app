import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  // Alert,
  AppState,
} from './components';

const Component = () => {
  return <AppState />;
};

export default Component;
