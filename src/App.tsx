import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  Alert,
} from './components';

const Component = () => {
  return <Alert />;
};

export default Component;
