import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  TextInput,
} from './components';

const Component = () => {
  return <TextInput />;
};

export default Component;
