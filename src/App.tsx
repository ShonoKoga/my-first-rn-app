import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  // Alert,
  // AppState,
  // AsyncStorage,
  // SafeAreaView,
  // Progress,
  Animation,
} from './components';

const Component = () => {
  return <Animation />;
};

export default Component;
