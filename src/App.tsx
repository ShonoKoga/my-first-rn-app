import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  Touchable,
} from './components';

const Component = () => {
  return <Touchable />;
};

export default Component;
