import React from 'react';
import {
  // Context,
  // Image,
  // ScrollView,
  // FlatList,
  // SectionList,
  // Touchable,
  // TextInput,
  // StatusBar,
  // ActivityIndicator,
  // Switch,
  // Alert,
  // AppState,
  // AsyncStorage,
  SafeAreaView,
} from './components';

const Component = () => {
  return <SafeAreaView />;
};

export default Component;
